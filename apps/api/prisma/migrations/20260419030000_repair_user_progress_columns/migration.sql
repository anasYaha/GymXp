ALTER TABLE "User"
ADD COLUMN IF NOT EXISTS "age" INTEGER,
ADD COLUMN IF NOT EXISTS "avatarUrl" TEXT,
ADD COLUMN IF NOT EXISTS "level" INTEGER NOT NULL DEFAULT 1,
ADD COLUMN IF NOT EXISTS "totalSessions" INTEGER NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "totalXp" INTEGER NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "currentStreak" INTEGER NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "levelXpProgress" INTEGER NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "levelDaysProgress" INTEGER NOT NULL DEFAULT 0;

ALTER TABLE "WorkoutSession"
ADD COLUMN IF NOT EXISTS "completionDay" TIMESTAMP(3);

CREATE UNIQUE INDEX IF NOT EXISTS "WorkoutSession_userId_completionDay_key"
ON "WorkoutSession"("userId", "completionDay");
