Placeholder assets for the future GymXP avatar system.

Planned contents:
- `.glb` base avatar models
- animation clips or references for `idle`, `wave`, `celebrate`, `flex`, `curl`, `squat`, and `run`
- texture and outfit variant assets

Keep this folder structure stable so a future avatar renderer can mount assets without moving feature code around.
