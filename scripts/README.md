# Scripts

Reserved for:
- local setup helpers
- seed runners
- demo reset scripts
- contract generation helpers

TODO: add `setup.ps1` and `seed-demo-data.ts` once dependencies are installed.

## Current Scripts

- `node scripts/test-dev-a.mjs`
  Runs a zero-dependency automated validation pass for the Developer A mobile scaffold.

- `npm run test:dev-a`
  Workspace shortcut for the same Developer A validation script.

